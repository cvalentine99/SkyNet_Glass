Settings page is rendering but the glass cards are not visible against the dark background. The form elements (inputs, selects, buttons) are present and functional. The "How It Works" section is below the fold. The page has all the right fields: Protocol, Router Address, Port, Stats Path, Test Connection, Auto-Polling toggle, Polling Interval slider, Save Configuration button. The glass cards need the grid-pattern background and the hero image overlay to match the dashboard. Also the "How It Works" section needs to be scrolled to.
